<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- libmpv declares minSdk 26. SiloPlaybackService gates MPV creation
         to API 26+ and uses Media3 on Android 7/API 24-25. -->
    <uses-sdk tools:overrideLibrary="dev.jdtech.mpv" />

    <!-- Phone and TV share one Play listing under the same applicationId.
         Requiring a touchscreen excludes this build from Android TV devices
         (which have none), so TVs only ever receive the leanback TV build. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- DownloadWorker runs as a foreground service while bytes are being
         streamed; Android 14+ requires the granular permission for dataSync. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".SiloApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Silo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="device"
                    android:scheme="silo" />
            </intent-filter>
        </activity>

        <!-- Media3 documents this as an exported service so system media
             controllers can discover and bind to the app session. -->
        <service
            android:name="org.siloserver.silo.common.player.SiloPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Disable WorkManager's androidx.startup auto-init so our explicit
             `WorkManager.initialize(this, workManagerConfiguration)` call in
             SiloApplication.onCreate wins with AppWorkerFactory wired
             in. Otherwise auto-init runs first with the default factory and
             custom workers (DownloadWorker) crash on instantiation. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- DownloadWorker calls setForeground with foregroundServiceType
             = dataSync on API 34+. Android 14 requires the granular type to
             be declared on the service element in the merged manifest;
             WorkManager's library manifest leaves it off, so we merge it
             on here. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
